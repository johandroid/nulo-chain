{
  "name": "zkp-revive-web-tester",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:artifacts": "node scripts/sync-artifacts.mjs",
    "dev": "vite --host 127.0.0.1",
    "build": "npm run sync:artifacts && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@polkadot/api": "^16.4.9",
    "@polkadot/extension-dapp": "^0.61.3",
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
